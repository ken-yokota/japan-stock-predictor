# 事前登録：未使用期間での確認

**書いた時刻: 2026-08-29 06:00 JST。この時点で対象期間の結果は一度も見ていません。**

この文書は実行前にコミットします。結果を見てから基準を書き換えないためです。
コミットハッシュが実行より前にあることで、それが確認できます。

---

## なぜ必要か

`docs/OOS_RESEARCH_REPORT_2026-08-28.md` の全結果は
**2025-08-05 〜 2026-08-14 の250営業日**で得たものです。
その期間で、私は次のことをしました。

- 6つのモデルアームを比較し、compact が良いと判断した
- 22の指標グループを Ablation / Incremental にかけた
- 7つのBUY条件と4つの確率閾値を比較した
- 5つの銘柄選択戦略を比較した
- 予測閾値の候補を4つ選んだ（0.8% が効くと分かった後で）

**この期間はもう「未使用のOOS」ではありません。** 何十回も見ています。
第3c節の +17.03% が本物かどうかは、一度も見ていない期間でしか判定できません。

---

## 対象期間

| | |
|---|---|
| 検証期間 | **2024-07-01 〜 2025-08-01**（約270営業日） |
| 学習窓 | 各予測日の直前120営業日（本番と同じ） |
| データ | `artifacts/cache/yahoo`（2023-11-06 から保持済み。追加取得なし） |
| 銘柄 | `config/stocks.yaml` の22銘柄（変更なし） |
| 特徴量 | `production_compact`（38列。前期間の比較で選んだもの） |
| コスト | 往復 0.200%（`config/trading.yaml` の値。変更しない） |

この期間の結果は、本文書のコミット時点で **一度も計算していません。**

---

## 検証する主張

### 主要主張（これが本命）

> **回帰の選別能力は、コストを払った後も、同数を無作為に選んだ場合を上回る。**

前期間では、試したすべての構成（候補グリッド3種）で、
同数を無作為に選んだ場合の95%点を20〜27ポイント上回りました。
グリッドを変えても動かなかった唯一の性質です。

**判定基準**（実行前に固定）:

- 予測閾値を毎日、過去だけを見て選び直す（分類器は使わない）
- 累積純リターンが、同数を無作為に選んだ2,000回の分布の **95%点を超えれば PASS**
- 超えなければ **FAIL**。「惜しかった」は FAIL とする

### 副次主張（参考。これで採否は決めない）

> 上記のルールは、往復0.200%を払って黒字になる。

前期間では候補グリッド次第で +17.03% 〜 -4.02% と大きく動きました。
**黒字にならなくても主要主張の判定は変わりません。**

---

## 使う候補グリッド（3種すべて報告する）

前期間で使ったものと同一。ひとつだけ選んで報告することはしません。

| 名前 | 候補 |
|---|---|
| A 後から選んだ4候補 | 0.3% / 0.5% / 0.8% / 1.0% |
| B 0.1%刻み20候補 | 0.1% 〜 2.0% |
| C 粗い5候補 | 0.0% / 0.5% / 1.0% / 1.5% / 2.0% |

**主要主張は3グリッドすべてで判定します。** 3つのうち1つだけが PASS した場合は
「3つ中1つ」と書きます。良かったものだけを報告することはしません。

---

## 同時に報告する対照

| 対照 | なぜ必要か |
|---|---|
| 全22銘柄を毎日買う | 市場そのものの動き |
| 現行の本番ルール（予測>0.3% かつ 確率>=0.60） | いま動いているもの |
| 同数を無作為に選んだ分布（5%/中央/95%） | 「取引を減らしただけ」との区別 |

---

## 実行前に決めておく、結果の書き方

- **PASS でも VALIDATED TRADING RULE とは呼びません。** 2期間で再現しても、
  それは2期間です。呼べるのは、本番で前向きに記録を取ってからです。
- **FAIL なら、第3c節の発見は前期間限りだったと書きます。** 期間を変えて
  探し直すことはしません。それは実績の良い期間を選ぶのと同じです。
- どちらでも、本番設定は変更しません。

---

## 追加の指標（記録するが判定には使わない）

方向的中率、順位IC（と t値）、Brier、最大ドローダウン、建玉数、取引日数。
