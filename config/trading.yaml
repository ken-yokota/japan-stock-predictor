version: 1

signal:
  predicted_intraday_return_threshold: 0.003
  probability_up_threshold: 0.60
  return_comparison: strict_greater_than
  probability_comparison: greater_than_or_equal
  insufficient_data_status: INSUFFICIENT_DATA

position:
  capital_per_stock_jpy: 1000000
  quantity_method: floor_capital_div_open
  # Tokyo cash equities normally trade in board lots of 100 shares.
  lot_size: 100
  lot_size_status: confirmed
  carry_overnight: false

costs:
  # Zero, by the operator's instruction on 2026-08-29: this is a paper test and
  # no order is being placed, so there is no commission and no slippage to
  # model. Every P&L figure the system produces is therefore a *gross* figure.
  #
  # If real trading starts, these must go back to the broker's actual rates
  # before any number here is read as a result. At 5 + 5 bps per side the round
  # trip was 0.20%, and it was about four fifths of the recorded loss.
  commission_bps_per_side: 0.0
  slippage_bps_per_side: 0.0
  assumptions_status: confirmed

prediction_price:
  morning_reference: previous_close
  recompute_after_actual_open: true
