# Runtime dependencies. Version ranges are mirrored in pyproject.toml.
alembic>=1.13,<2
exchange-calendars>=4.6,<5
httpx>=0.27,<1
lightgbm>=4.5,<5
numpy>=2.0,<3
pandas>=2.2,<3
plotly>=5.22,<7
psycopg[binary]>=3.2,<4
pydantic>=2.8,<3
pydantic-settings>=2.4,<3
PyYAML>=6,<7
resend>=2.4,<3
scikit-learn>=1.5,<2
SQLAlchemy>=2.0.31,<3
statsmodels>=0.14,<1
# Pin the runtime exercised by local AppTest, Python 3.14 CI and Cloud.
# Updating this manifest also rebuilds Cloud instead of retaining imported
# modules and cached service instances across the audit deployment.
streamlit==1.64.0
xgboost>=2.1,<4
yfinance>=1.5,<2
